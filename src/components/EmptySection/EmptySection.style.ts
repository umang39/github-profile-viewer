import { makeStyles } from "@material-ui/core";
import { grey } from "@mui/material/colors";

const useEmptyBioStyle = makeStyles({
  text: {
    color: grey[400],
  },
});
export default useEmptyBioStyle;
