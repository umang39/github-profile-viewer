import { makeStyles } from "@material-ui/core";
import { theme } from "../../theme";

const useUserProfile = makeStyles({
  card: {
    marginTop: theme.spacing(10),
  },
  loader: {
    marginTop: theme.spacing(10),
  },
});

export default useUserProfile;
